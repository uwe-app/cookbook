lang = "en"
host = "sass-build-tool.uwe.app"

[dependencies]
"std::core" = "^5"

[style]
src = "/assets/styles/main.css"
dynamic = true

[[hook]]
command = "npm"
args = ["run", "build"]
files = ["src/*.scss"]
watch = true
